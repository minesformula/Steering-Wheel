#include <Adafruit_NeoPixel.h>

#ifndef NEOPIXEL_H
#define NEOPIXEL_H

#endif //NEOPIXEL_H

/*
// LED globals
#define PIN 6    
#define NUMPIXELS 16
Adafruit_NeoPixel pixels(NUMPIXELS, PIN, NEO_GRB + NEO_KHZ800);

uint32_t NONE = ((uint32_t)0 << 16) | ((uint32_t)0 <<  8) | 0;
uint32_t RED = ((uint32_t)255 << 16) | ((uint32_t)0 <<  8) | 0;
uint32_t GREEN = ((uint32_t)0 << 16) | ((uint32_t)255 <<  8) | 0;
uint32_t BLUE = ((uint32_t)0 << 16) | ((uint32_t)0 <<  8) | 255;
uint32_t YELLOW = ((uint32_t)255 << 16) | ((uint32_t)255 <<  8) | 0;
uint32_t ORANGE = ((uint32_t)255 << 16) | ((uint32_t)128 <<  8) | 0;
uint32_t LTBLUE = ((uint32_t)0 << 16) | ((uint32_t)255 <<  8) | 255;

// LED setup
    pixels.begin(); // INITIALIZE NeoPixel strip object (REQUIRED)
    pixels.clear(); 
    pixels.setBrightness(BRIGHTNESS);
    pixels.show();

    // initialize communication (show that attempting Can Connection)
    // stays on splash screen and signals with LEDs
    pixels.setPixelColor(0, BLUE);
    pixels.setPixelColor(1, BLUE);
    pixels.setPixelColor(14, BLUE);
    pixels.setPixelColor(15, BLUE);
    pixels.show();

*/
